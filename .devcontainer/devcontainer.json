{
  "name": "swc-plugin-de-indent-template-literal",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-20-bullseye",
  "postCreateCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | bash -s -- -y && . \"$HOME/.cargo/env\" && rustup install nightly && rustup default nightly && cargo install swc_cli && rustup target add wasm32-wasi",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "streetsidesoftware.code-spell-checker",
        "EditorConfig.EditorConfig",
        "dbaeumer.vscode-eslint",
        "GitHub.copilot",
        "GitHub.copilot-chat",
        "eamodio.gitlens",
        "esbenp.prettier-vscode"
      ]
    }
  }
}
