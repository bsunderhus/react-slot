{
  "name": "react-sockets-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:types": "turbo build:types",
    "build:api": "turbo build:api",
    "prepack": "turbo prepack",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "turbo": "2.0.4"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=18"
  }
}
