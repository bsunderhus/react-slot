{
  "name": "swc-plugin-react-slot",
  "version": "0.0.1",
  "description": "SWC plugin to transform slot elements into properties",
  "author": "Bernardo Sunderhus <bernardo.sunderhus@gmail.com>",
  "license": "ISC",
  "keywords": [
    "swc-plugin"
  ],
  "main": "swc_plugin_react_slot.wasm",
  "files": [
    "swc_plugin_react_slot.wasm"
  ],
  "scripts": {
    "build": "cargo build-wasi --release",
    "prepack": "npm run build && cp target/wasm32-wasi/release/swc_plugin_react_slot.wasm ."
  }
}
